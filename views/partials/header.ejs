<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= title || "Storage App" %></title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <style>
    :root{
      --brand:#2b6ef6;
      --muted:#6b7280;
    }
    body{ font-family:"Poppins",sans-serif; background:#f6f8fb; color:#1f2937; }
    .card{ border-radius:14px; }
    .btn-primary{ background:var(--brand); border:0; }
    .small-muted{ color:var(--muted); }
    .shadow-soft{ box-shadow: 0 6px 20px rgba(40,41,61,0.06); }
    .container-max{ max-width:1100px; margin:auto; }
    .nav-brand{ font-weight:600; letter-spacing:0.2px; color:#fff; }
  </style>
</head>
<body>
